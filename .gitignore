.metals
.bloop
.luarc.json
# nvim 설정용 lua_ls 설정은 편집기와 script/nvim-luals-check.sh가 공유해야 하므로 추적한다.
# (위 .luarc.json 무시는 도구가 레포 루트에 자동 생성하는 것을 막기 위한 것)
!nvim/.luarc.json
.bsp/
.scala-build/
# lazy.nvim이 :Lazy update마다 새로 쓰는 파일이다. 한때 시점 복구를 위해 추적했지만
# (a3b69f3) 업데이트가 잦아 working tree가 상시 더러워져 추적을 그만뒀다.
nvim/lazy-lock.json
