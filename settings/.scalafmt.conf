# ~/.scalafmt.conf — 프로젝트에 .scalafmt.conf가 없을 때만 쓰이는 전역 폴백.
# scalafmt 3.x는 version이 없으면 실행을 거부하므로 반드시 함께 둔다.
# 설치된 CLI와 같은 값으로 맞춰 추가 다운로드를 피한다 (scalafmt --version으로 확인).
version = "3.10.7"
runner.dialect = scala213

maxColumn = 120
